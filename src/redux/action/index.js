import { addNewFlashCard, removeFlashCard } from "./flashCard";
import { setGroupIdToDownload, setCardIdToDownload } from "./downloadCard";
import { setCardRefToPrint } from "./printCard";
import { setCloseModal, setOpenModal } from "./setModal";

// These are all the action creators for different actions that can be used in the application.
export {
  addNewFlashCard,
  removeFlashCard,
  setGroupIdToDownload,
  setCardIdToDownload,
  setCardRefToPrint,
  setOpenModal,
  setCloseModal,
};
